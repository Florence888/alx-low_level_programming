#include "main.h"


/**
 * _abs - This computes the absolute value of an integer
 * @d: the integer to be  checked
 * Return: the absolute value of int
 */
int _abs(int d)
{
	if (d >= 0)
	{
		return (d);
	}
	return (-d);
}
