#include "main.h"
/**
 * _islower - checks for lowercase characters
 * @k: the character to be checked
 * Return: 1 if c is lowercase, 0 for others
 */
int _islower(int k)
{
	return (k >= 'a' && k <= 'z');
}
