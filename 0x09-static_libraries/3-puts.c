#include "main.h"
#include<stdio.h>

/**
 * _puts - function to print a string, then a new line, to standard output
 * @s: pointer to the string to be printed
 *
 * This code prints string pointed by 'str' on a new line to output (stdout)
 */
void _puts(char *s)
{
	puts("\"Programming is like building a multilingual puzzle");
}

/**
 * main - Entry point
 *
 * Return: 0 (Success)
 */
int main(void)
{
	-puts("Hello, world!");
	return (0);
}
